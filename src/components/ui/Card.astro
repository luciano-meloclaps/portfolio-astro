---
// src/components/ui/Card.astro
interface Props {
  class?: string;
  padding?: 'sm' | 'md' | 'lg' | 'none';
}
const { class: className, padding = 'md' } = Astro.props;

const paddingClasses = {
  sm: 'p-4',
  md: 'p-6',
  lg: 'p-8',
  none: 'p-0',
};
---

<div
  class:list={['glass-panel rounded-large', paddingClasses[padding], className]}
>
  <slot />
</div>
